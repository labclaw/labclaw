"""Tier B: Temporal knowledge graph — structured entities and relationships.

Wraps Graphiti for bi-temporal edge tracking, entity resolution,
multi-tenant isolation (group_id), and semantic search over the
lab's structured knowledge (people, protocols, experiments, devices,
findings, decisions, failures).
"""
